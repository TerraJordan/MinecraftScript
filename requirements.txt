pyautogui
opencv-python
numpy
pytz
pygetwindow
pynput
pytest
pystray
Pillow
plyer

# pynput используется для точного удержания клавиш (movement.py) и для
# глобальной горячей клавиши старта/остановки (hotkey.py) — она реагирует
# ТОЛЬКО на одну заданную комбинацию и не используется для глобального
# перехвата произвольного ввода. recorder.py по-прежнему работает через
# явные нажатия кнопок в GUI, а не через фоновый хук.
# pytest нужен только для запуска tests/ и не требуется на "боевой" машине,
# где просто работает бот.
